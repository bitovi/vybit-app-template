{
  "servers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--browser",
        "chromium",
        "--no-sandbox",
        "--caps=vision"
      ]
    },
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${workspaceFolder}"
      ]
    },
    "convey": {
      "type": "stdio",
      "command": "npx",
      "args": ["@bitovi/convey@latest"],
      "cwd": "${workspaceFolder}/mock-app",
      "env": {
        "CONVEY_DEBUG": "true"
      }
    }
  }
}
