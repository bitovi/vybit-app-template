We are going to build a site that dynamically responds, by changing styles like:

- font sizes
- line heights
- padding

dynamically in an attempt to make the page best fit the screen both vertically and horizontally.


In order to do this, we first have to get some basic things working.

I'd like to get a page like [layout.png](layout.png) working. 

It will need to always scale to match the viewport dimensions.  

This means that Expand One should always be at the top of the page and Expand Five at the bottom.

Users should be able to click any of the expands and they will show their content area (and close the currently showing content area). This is like an accordion.


Lets first do some research on how to achieve this.  